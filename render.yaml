services:
  - type: web
    name: mcp-hello-bye-card
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist-server/main.js
    envVars:
      - key: NODE_VERSION
        value: "20"
